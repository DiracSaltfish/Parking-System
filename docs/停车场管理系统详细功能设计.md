# 停车场管理系统详细功能设计

## 1. 项目目标

本项目是一个双角色的停车场管理系统，采用前后端分离实现：

- 前端主版本：`HTML + CSS + JavaScript`
- 前端保留版本：`Vue 3 + Element Plus`
- 后端：`Spring Boot + Java`
- 数据存储：`Redis`

系统角色：

- 管理员
- 普通用户

当前目标是优先完成一条完整、稳定、可演示的业务主线，并满足“前端不使用框架”的课程要求。

---

## 2. 功能状态总览

### 2.1 已完成功能

- 原生前端版本完成
- 管理员登录
- 普通用户注册 / 登录
- 前端统一登录入口自动分流
- 用户绑定、删除车牌
- 用户查询当前停车信息
- 用户查询停车记录
- 用户查询缴费记录
- 用户模拟缴费
- 管理员查看当前在场车辆
- 管理员查看历史停车记录
- 管理员按车位类型录入车辆入场
- 管理员办理车辆出场
- 车位管理与占用状态同步
- Redis 演示数据初始化

### 2.2 当前为骨架功能

- 管理员停车费豁免
- 管理员仪表盘实时统计
- 收费规则真实持久化

---

## 3. 角色与功能划分

## 3.1 管理员功能

管理员进入后台后，可以完成以下操作：

- 登录系统
- 查看停车场总体运行情况
- 查看当前所有停放车辆
- 查看历史停车记录
- 查看车辆的停车时长、费用、支付状态
- 按车位类型手动录入车辆入场信息
- 办理车辆出场
- 查看车位占用状态同步结果
- 后续支持停车费豁免
- 后续支持收费规则设置

## 3.2 普通用户功能

普通用户进入系统后，可以完成以下操作：

- 注册 / 登录
- 绑定自己的车牌号
- 查看自己当前是否在场
- 查询自己的停车时长
- 查询当前待缴费用
- 在线模拟缴费
- 查看历史停车记录
- 查看历史缴费记录

---

## 4. 页面设计

### 4.1 公共页面

- 登录页
- 注册页

当前课程汇报主版本页面位于 `frontend-native/`，使用原生 `HTML + CSS + JavaScript` 实现。

### 4.2 管理员端页面

- 管理员首页 / 仪表盘
- 当前停车车辆页
- 历史停车记录页
- 车位管理页
- 收费规则页

### 4.3 用户端页面

- 用户首页
- 我的车辆页
- 当前停车信息页
- 停车记录页
- 缴费记录页

---

## 5. 核心业务设计

### 5.1 统一登录入口

当前前端登录页采用单入口设计：

- 输入账号和密码
- 当账号为 `admin` 时，进入管理员登录逻辑
- 其他账号默认走普通用户登录逻辑
- 登录成功后根据 `role` 跳转页面

当前这套逻辑已经在原生前端版本中实现，不依赖前端框架。

### 5.2 管理员登录

管理员使用账号密码登录后台。

登录后返回：

- 用户基本信息
- 角色信息
- Token

系统根据角色进入管理员端页面。

### 5.3 普通用户注册与登录

普通用户支持：

- 用户名注册
- 用户名密码登录

第一版不接短信验证，降低实现复杂度。

### 5.4 用户绑定车牌

普通用户可以绑定车辆。

当前支持：

- 查询绑定车辆
- 新增绑定车辆
- 删除绑定车辆

业务约束：

- 同一个车牌只能绑定一个用户
- 车牌格式需要通过前端与后端校验

### 5.5 管理员录入车辆入场

当前已实现。

流程如下：

1. 管理员输入车牌号
2. 选择车位类型
3. 系统检查该车是否已经在场
4. 如果车牌已绑定用户，则自动关联用户
5. 如果未绑定用户，则按临时车辆处理
6. 系统从所选类型中自动分配一个空闲车位
7. 系统生成停车记录
8. 以当前时刻作为入场时间开始计费
9. 车位管理页面同步更新该车位为占用状态

说明：

- 普通车位自动分配 `A-*`
- 新能源车位自动分配 `B-*`
- VIP 车位自动分配 `C-*`
- 录入完成后，当前停车列表和车位列表会立即刷新

### 5.6 管理员办理车辆出场

当前已实现基础流程。

流程如下：

1. 管理员在当前停车列表中点击办理出场
2. 系统补齐出场时间
3. 当前停车记录状态改为 `COMPLETED`
4. 对应车位状态同步恢复为 `FREE`

### 5.7 用户查询当前停车信息

普通用户登录后，可以查看自己当前车辆是否在停车场内。

如果车辆在场，会展示：

- 车牌号
- 当前车位
- 入场时间
- 已停车时长
- 当前应缴金额
- 支付状态

如果当前没有在场车辆，会返回“当前没有在场车辆”。

### 5.8 用户模拟缴费

当前已实现模拟支付流程，不接第三方支付平台。

流程如下：

1. 用户选择当前停车记录
2. 选择支付方式
3. 后端更新支付状态为已支付
4. 写入支付时间
5. 生成支付记录

这个方案适合课程项目演示，也能体现完整业务流程。

### 5.9 管理员查看全部车辆

管理员可以在“当前停车车辆”页面查看：

- 当前所有在场车辆
- 车牌号
- 车主名称或“临时车辆”
- 当前车位
- 入场时间
- 已停车时长
- 当前费用
- 支付状态

当前已支持：

- 当前列表实时刷新
- 办理出场
- 录入入场后状态同步

### 5.10 停车记录查询

当前管理员和普通用户都可以查询停车记录：

- 用户只能查自己的记录
- 管理员可以查全部记录

可展示字段：

- 记录编号
- 车牌号
- 入场时间
- 出场时间
- 停车时长
- 原始费用
- 最终费用
- 支付状态
- 记录状态

### 5.11 停车费豁免

这是系统中的扩展亮点功能。

当前设计已完成，但真实业务流程仍在下一阶段：

- 全额豁免
- 部分减免
- 原始费用与最终费用区分
- 记录操作人和操作时间

建议后续保留字段：

- `discountType`
- `discountAmount`
- `finalAmount`
- `exemptReason`
- `operatorId`
- `operateTime`

---

## 6. 当前已打通的演示主流程

推荐演示顺序：

1. 打开统一登录页
2. 管理员登录
3. 查看当前停车列表
4. 按车位类型录入一辆新车入场
5. 展示系统自动分配真实车位并开始计费
6. 展示车位管理页面同步变更为占用状态
7. 演示管理员办理出场，车位恢复为空闲
8. 退出后，普通用户登录
9. 查看绑定车辆
10. 查看当前停车信息
11. 查看停车记录和缴费记录
12. 完成模拟缴费

---

## 7. 当前存在的限制

- 费用豁免还未接真实业务
- 仪表盘当前为静态演示数据
- 收费规则当前仍为骨架
- 密码暂时还是演示用明文存储，后续应改为 `BCrypt`

---

## 8. 后续开发优先级建议

### 第一优先级

- 管理员费用豁免

### 第二优先级

- 收费规则接 Redis
- 仪表盘接 Redis

### 第三优先级

- 密码哈希
- 敏感信息加密
- 数据统计优化
